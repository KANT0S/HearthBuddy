namespace Triton.Game.Mapping
{
    using ns26;
    using System;
    using Triton.Game.Mono;

    [Attribute38("UriUtils")]
    public class UriUtils : MonoClass
    {
        public UriUtils(IntPtr address) : this(address, "UriUtils")
        {
        }

        public UriUtils(IntPtr address, string className) : base(address, className)
        {
        }
    }
}

