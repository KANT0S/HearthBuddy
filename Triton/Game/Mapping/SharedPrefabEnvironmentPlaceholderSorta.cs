namespace Triton.Game.Mapping
{
    using ns26;
    using System;

    [Attribute38("SharedPrefabEnvironmentPlaceholderSorta")]
    public class SharedPrefabEnvironmentPlaceholderSorta : MonoBehaviour
    {
        public SharedPrefabEnvironmentPlaceholderSorta(IntPtr address) : this(address, "SharedPrefabEnvironmentPlaceholderSorta")
        {
        }

        public SharedPrefabEnvironmentPlaceholderSorta(IntPtr address, string className) : base(address, className)
        {
        }

        public void Start()
        {
            base.method_8("Start", Array.Empty<object>());
        }

        public void Update()
        {
            base.method_8("Update", Array.Empty<object>());
        }
    }
}

